[project]
name = "paper-ai-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "arxiv>=1.4.0",
    "dotenv>=0.9.9",
]
